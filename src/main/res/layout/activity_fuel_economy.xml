<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0E1A"
    tools:context=".ui.activities.FuelEconomyActivity">

    <!-- Premium Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_fuel"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#161B22"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:title="Fuel Economy"
        app:titleTextColor="#FFFFFF" />

    <!-- Glassmorphic Tab Container -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabs_fuel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#161B22"
        app:layout_constraintTop_toBottomOf="@id/toolbar_fuel"
        app:tabIndicatorColor="#0056B3"
        app:tabSelectedTextColor="#0056B3"
        app:tabTextColor="#8B949E">

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="DAILY" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="WEEKLY" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MONTHLY" />

    </com.google.android.material.tabs.TabLayout>

    <!-- Content ViewPager -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/pager_fuel"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tabs_fuel" />

</androidx.constraintlayout.widget.ConstraintLayout>
